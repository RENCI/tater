{
  "spec_version": "1.0",
  "title": "tater - span annotation",
  "description": "Tag pet-related entities with explicit labels, descriptions, and entity types.",
  "instructions": "Highlight relevant text spans in the document, then click an entity type to label the span.",
  "data_schema": [
    {
      "id": "entities",
      "type": "span_annotation",
      "entity_types": ["Pet", "Breed", "Behavior", "Activity"],
      "label": "Pet Entities",
      "description": "Highlight text then click an entity type to label it."
    },
    {
      "id": "quality",
      "type": "choice",
      "options": ["high", "medium", "low"],
      "required": true,
      "label": "Record Quality"
    }
  ]
}
