{
  "spec_version": "1.0",
  "title": "tater - simple (mixed)",
  "description": "Pet mood shown as a vertical radio group; attention uses the default widget.",
  "instructions": "Select the pet's sentiment using the radio group explicitly. The 'Needs Attention' field auto-generates.",
  "data_schema": [
    {
      "id": "pet_mood",
      "type": "choice",
      "options": [
        "happy",
        "anxious",
        "calm"
      ],
      "widget": {
        "type": "radio_group",
        "label": "Pet Mood",
        "description": "Overall mood of the pet in this record",
        "required": true,
        "orientation": "vertical"
      }
    },
    {
      "id": "needs_attention",
      "type": "boolean",
      "widget": {
        "type": "checkbox",
        "label": "Needs Attention?",
        "description": "Does this pet require immediate attention?"
      }
    }
  ]
}