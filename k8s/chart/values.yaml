# Default values for tater.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: containers.renci.org/clin-ann/tater
  # This is ignored on Sterling, which uses Always regardless
  pullPolicy: Always
  # Empty string uses Chart.appVersion; override with --set image.tag=<version>
  tag: ""

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext:
  runAsUser: 10001
  runAsNonRoot: true
  fsGroup: 10001

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

service:
  type: ClusterIP
  port: 8050

env: []
  # Additional environment variables for the tater container
  # Example:
  # - name: CUSTOM_VAR
  #   value: "custom-value"

ingress:
  enabled: true
  className: ""
  annotations:
    # cert-manager.io/cluster-issuer: letsencrypt
    cert-manager.io/cluster-issuer: zerossl
    # TODO: restrict to RENCI/UNC ranges — email help@renci.org for tater.apps.renci.org exception first
    # nginx.ingress.kubernetes.io/whitelist-source-range: "152.2.0.0/16,152.19.0.0/16,204.85.176.0/20,204.84.8.0/22,204.84.252.0/22,198.85.230.0/23,2606:f640::/32,2602:fcfb::/36,2600:2701:4000::/44,2610:28:3090::/48"
    # nginx.ingress.kubernetes.io/whitelist-source-range: "0.0.0.0/0,::/0"
  hosts:
    - host: tater.apps.renci.org
      paths:
        - path: /
          pathType: Prefix
  tls:
   - secretName: tater.apps.renci.org-tls
     hosts:
       - tater.apps.renci.org

  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
