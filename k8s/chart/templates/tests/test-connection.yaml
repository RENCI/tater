apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tater.fullname" . }}-test-connection"
  labels:
    {{- include "tater.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tater.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
